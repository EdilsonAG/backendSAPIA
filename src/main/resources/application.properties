# --- Servidor ---
server.port=8081

# --- CORS ---
app.cors.allowed-origin=http://localhost:5173

# --- SAP ---
sap.base-url=https://snp.copacol.com.br:8001/sap/opu/odata/sap
sap.client=001
#ia.server.base-url=http://10.1.26.242:8080
#ia.server.base-url=http://10.46.2.94:8080
ia.server.base-url=http://localhost:8080

# --- Sessao (equivalente ao SESSION_COOKIE_NAME / SESSION_TTL_MINUTES do Express) ---
server.servlet.session.cookie.name=app_session
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax
server.servlet.session.timeout=30m
spring.session.timeout=30m
spring.session.store-type=redis


 
# --- Redis ---
spring.data.redis.host=localhost
spring.data.redis.port=6379
#spring.data.redis.password=Ed.183729
